import dynamic from 'next/dynamic';
import Head from 'next/head'
import { Layout } from '../components/Layout';

const Home = () => {

	return (
		<Layout>
			<div>Home</div>
		</Layout>
	)
}

export default Home
